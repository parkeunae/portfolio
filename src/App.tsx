import React from 'react';
import Header from './components/Header/Header';
import './App.scss';

const Root = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default Root;